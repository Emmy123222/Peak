"use client"; // Add this only if using hooks inside ContactUsPage

import ContactUsPage from "@/components/landing/ContactUsPage";

export default function ContactPage() {
  return <ContactUsPage />;
}
